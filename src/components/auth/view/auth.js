import authHeader from '@/components/auth/vue/authHeader.vue';
import authBody from '@/components/auth/vue/authBody.vue';

export default {
  components: {
    'auth-header': authHeader,
    'auth-body': authBody,
  },
};
